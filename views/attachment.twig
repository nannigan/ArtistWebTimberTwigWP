{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">

               {#  attachment page
                {{dump(post.prev.link)}} #}

               {#  <pre>{{dump(post.guid)}}</pre> #}
                <img class="portfolio-image" src="{{post.guid}}">
                {# class="article-title" #}
                 <h3>{{ post.title }}  </h3><p class='caption'>{{ post.caption }}</p>



			</section>

		</article>
	</div><!-- /content-wrapper -->
{% endblock %}
