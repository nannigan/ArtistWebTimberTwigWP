{% extends "base.twig" %}
{% block content %}
    <div class="content-wrapper">
        <article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
            {# <h4>Contact</h4> #}
            <div class='row'>
                <div class='article-body contact'>
                    <div class="col-6">
                       You can <a class='sayHi' href='#'>contact Liz</a> at lizleg2[at]sonic[dot]net (replacing the dot and at with the email equivalents).
                    </div>
                </div>
            </div>
        </article>
        </div><!-- /content-wrapper -->
    {% endblock %}