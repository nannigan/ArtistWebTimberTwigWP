{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">
				<h1>{{post.title}}</h1>
                 <pre>{{dump(post.post_type)}}</pre>
				<div class="article-body">

                    <pre>{{dump(post)}}</pre>
                   {#  <pre>{{dump(post)}}</pre> {# debug #}
				</div>
			</section>
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}